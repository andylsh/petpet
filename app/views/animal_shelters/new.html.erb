<%= form_for @company do |form| %>
		
		<%= render partial: '/animal_shelters/form', object: form %>

		<%= hidden_field(:user, :animal_shelter_id) %>
		
        <%= form.submit "create" %>
<% end %>
		
		